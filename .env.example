# Claude Code Slack Notifier Configuration
# Copy this file to .env and fill in your actual values
# Then source it: source .env

# ============================================================================
# OPTION 1: Direct Message Mode (Recommended)
# ============================================================================
# Send notifications as direct messages to your Slack account
# Requires: pip install slack-sdk

# Slack Bot User OAuth Token (starts with xoxb-)
# Get it from: https://api.slack.com/apps -> Your App -> OAuth & Permissions
SLACK_CLAUDE_CODE_BOT_TOKEN=xoxb-your-bot-token-here

# Your Slack Member ID (starts with U)
# Get it from: Click your profile in Slack -> Copy member ID
# Or use: https://api.slack.com/methods/users.list/test
SLACK_MEMBER_ID=U01234567


# ============================================================================
# OPTION 2: Webhook Mode
# ============================================================================
# Send notifications to a Slack channel via Incoming Webhooks
# No additional dependencies required

# Slack Incoming Webhook URL
# Get it from: https://api.slack.com/apps -> Your App -> Incoming Webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL


# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env and fill in your actual values
#
# 3. Load environment variables:
#    source .env
#
# 4. Or add to your shell profile (~/.bashrc or ~/.zshrc):
#    export SLACK_CLAUDE_CODE_BOT_TOKEN="xoxb-..."
#    export SLACK_MEMBER_ID="U..."
#    # OR
#    export SLACK_WEBHOOK_URL="https://hooks.slack.com/..."
#
# 5. Test the configuration:
#    # For DM mode:
#    echo '{}' | python3 ~/bin/claude_slack_notifier.py --event-type stop --mode dm
#
#    # For Webhook mode:
#    echo '{}' | python3 ~/bin/claude_slack_notifier.py --event-type stop --mode webhook
#
# ============================================================================
